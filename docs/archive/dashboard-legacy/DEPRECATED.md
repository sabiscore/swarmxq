# DEPRECATED — archived dashboard-legacy/

This directory contains the legacy fallback static dashboard.  
It is retained for reference only.

**Canonical dashboard:** `apps/swarmx-dashboard/` (Next.js 16 + React 19)

```bash
# Run the canonical dashboard:
pnpm --filter @swarmx/dashboard dev
# or via Makefile:
make dashboard
```

**Planned removal:** Phase 4 (legacy dashboard retirement milestone).

See `ARCHITECTURE.md` and `docs/` for the canonical topology.
